{
  "name": "christianle",
  "version": "3.0.0-alpha.1",
  "description": "http://christianle.com",
  "scripts": {
    "slate": "rm -rf node_modules && npm run clean && yarn install",
    "clean": "rm -rf dist",
    "dev": "NODE_ENV=development node index.js --progress --colors --profile",
    "start": "NODE_ENV=production node index.js --progress --colors --profile",
    "build": "npm run clean && NODE_ENV=production webpack -p --progress --colors --profile",
    "test": "echo \"Error: no test specified\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cle1994/Portfolio.git"
  },
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
  }
}
